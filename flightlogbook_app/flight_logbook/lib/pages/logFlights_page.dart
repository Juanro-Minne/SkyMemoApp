import 'package:flutter/material.dart';

class LogFlights extends StatelessWidget {
  const LogFlights({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('This is log flights page'));
  }
}
