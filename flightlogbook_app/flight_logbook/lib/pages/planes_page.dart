import 'package:flutter/material.dart';

class LogPlanes extends StatelessWidget {
  const LogPlanes({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('This is planes page'));
  }
}
