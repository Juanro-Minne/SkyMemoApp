import 'package:flutter/material.dart';

class LogFlightsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Log Flights')),
      body: Center(child: Text('hello Flights Screen')),
    );
  }
}
